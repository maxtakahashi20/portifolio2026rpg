/** Login público — quem souber o segredo segue para projetos. */
export const ADMIN_LOGIN_ROUTE = "/admin";

/** Painel de projetos — só após gravar o segredo na sessão (via /admin). */
export const ADMIN_PROJECTS_ROUTE = "/admin/projetos";

export const ADMIN_SECRET_STORAGE_KEY = "vale-ecos-admin-secret";
